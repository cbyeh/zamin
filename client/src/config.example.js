/** Example s3 bucket, remove this file during production.
 *  To keep key secret, the real config.js is ignored in repo for now.
 *  TODO: For production, to keep key secret, make app request data through backend
 */

const config = {
  bucketName: 'myBucket',
  albumName: 'images',
  region: 'eu-west-1',
  accessKeyId: 'EXAMPLEKEYAEIN5234NASD',
  secretAccessKey: 'secretexample789499+DEkg8dsdsaasgf210437810',
};

export default config;
