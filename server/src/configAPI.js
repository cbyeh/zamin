/* Put any api keys here, add to .gitignore if they must be discreet */

exampleApi1 = {
  username: 'userName',
  key: 'keyHere',
};

const key = 'exampleKey';

module.exports = {
  api1: exampleApi1,
  key: key,
};
